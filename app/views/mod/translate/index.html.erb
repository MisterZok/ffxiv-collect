<% title(t('mod.translation_dashboard')) %>

<div class="card mb-3">
  <h5 class="card-header d-flex justify-content-between align-items-center">
    <%= link_to t('mod.manage_translations'), mod_translate_path, class: 'unstyled' %>
    <%= search_form_for(@q, url: mod_translate_path, enforce_utf8: false,
                        class: 'form form-inline search-form d-flex') do |f| %>
      <%= f.collection_select :type_id_eq, @source_types, :id, :name,
        { include_blank: t('all.sources') }, class: 'form-control form-control-sm' %>
      <%= f.submit t('search'), name: nil, class: 'btn btn-secondary btn-sm mr-0' %>
    <% end %>
  </h5>
  <div class="card-body card-table">
    <table class="table table-striped table-sm">
      <tr>
        <th></th> <!-- Image -->
        <th></th> <!-- Icon -->
        <th><%= t('name') %></th>
        <th><%= t('source') %></th>
        <th></th> <!-- Actions -->
      </tr>
      <% @sources.each do |source| %>
        <tr>
          <td><%= collectable_image_link(source.collectable) %></td>
          <td><%= collectable_icon(source.collectable) %></td>
          <td>
            <%= source.collectable.name %>
            <%= gender_symbol(source.collectable.try(:gender)) %>
          </td>
          <td><%= source.text_en %></td>
          <td class="text-center">
            <%= link_to fa_icon('pen'), polymorphic_url([:mod, source.collectable], action: :edit),
              class: 'btn btn-secondary btn-sm' %>
          </td>
        </tr>
      <% end %>
    </table>
  </div>
  <div class="card-footer d-flex justify-content-center mb-0">
    <% if @sources.size == 0 %>
      <h1>
        <%= fa_icon('birthday-cake') %>
      </h1>
    <% else %>
      <%= will_paginate(@sources) %>
    <% end %>
  </div>
</div>
